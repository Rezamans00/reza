# reza

This repository contains a simple command-line chess game implemented in Python.

## Playing the Game

Run the game using Python:

```bash
python3 -m chess_game
```

Moves are entered using algebraic coordinates in the format `e2e4` (from e2 to e4). Type `quit` or `exit` to stop playing.
